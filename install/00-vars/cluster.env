HOST="${HOST:-dj-tw-mano-cpm01-vm-bastion}"
CLUSTER_NAME="${CLUSTER_NAME:-lgu}"
BASE_DOMAIN="${BASE_DOMAIN:-dj-cpm01.co.kr}"

API_SERVER="${API_SERVER:-https://api.${CLUSTER_NAME}.${BASE_DOMAIN}:6443}"
BASTION_HOST_PATTERN="${BASTION_HOST_PATTERN:-bastion}"

OFFLINE="true"
